{
	"name": "rmt-debrid",
	"private": true,
	"scripts": {
		"build:frontend": "cd frontend && bun vite build",
		"check:frontend": "bun run --cwd frontend check",
		"test:backend": "bun start-backend.js -m unittest discover -s tests -v",
		"start": "bun start-backend.js --build-frontend"
	}
}
